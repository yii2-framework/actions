---
on:
  - pull_request
  - push

name: linter

permissions:
  checks: write
  contents: read
  statuses: write

jobs:
  linter:
    uses: ./.github/workflows/super-linter.yml@initial-release
    secrets:
      AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
